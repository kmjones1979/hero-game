/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findPlayerPda } from "../pdas";
import { HERO_GAME_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const MINT_HERO_DISCRIMINATOR = new Uint8Array([
  199, 91, 38, 207, 135, 141, 18, 119,
]);

export function getMintHeroDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(MINT_HERO_DISCRIMINATOR);
}

export type MintHeroInstruction<
  TProgram extends string = typeof HERO_GAME_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountPlayer extends string | AccountMeta<string> = string,
  TAccountHero extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountPlayer extends string
        ? WritableAccount<TAccountPlayer>
        : TAccountPlayer,
      TAccountHero extends string
        ? WritableAccount<TAccountHero>
        : TAccountHero,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type MintHeroInstructionData = {
  discriminator: ReadonlyUint8Array;
  clientSeed: bigint;
};

export type MintHeroInstructionDataArgs = { clientSeed: number | bigint };

export function getMintHeroInstructionDataEncoder(): FixedSizeEncoder<MintHeroInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["clientSeed", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: MINT_HERO_DISCRIMINATOR }),
  );
}

export function getMintHeroInstructionDataDecoder(): FixedSizeDecoder<MintHeroInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["clientSeed", getU64Decoder()],
  ]);
}

export function getMintHeroInstructionDataCodec(): FixedSizeCodec<
  MintHeroInstructionDataArgs,
  MintHeroInstructionData
> {
  return combineCodec(
    getMintHeroInstructionDataEncoder(),
    getMintHeroInstructionDataDecoder(),
  );
}

export type MintHeroAsyncInput<
  TAccountUser extends string = string,
  TAccountPlayer extends string = string,
  TAccountHero extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  player?: Address<TAccountPlayer>;
  hero: Address<TAccountHero>;
  systemProgram?: Address<TAccountSystemProgram>;
  clientSeed: MintHeroInstructionDataArgs["clientSeed"];
};

export async function getMintHeroInstructionAsync<
  TAccountUser extends string,
  TAccountPlayer extends string,
  TAccountHero extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HERO_GAME_PROGRAM_ADDRESS,
>(
  input: MintHeroAsyncInput<
    TAccountUser,
    TAccountPlayer,
    TAccountHero,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  MintHeroInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountPlayer,
    TAccountHero,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? HERO_GAME_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: true },
    hero: { value: input.hero ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.player.value) {
    accounts.player.value = await findPlayerPda({
      user: expectAddress(accounts.user.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.hero),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getMintHeroInstructionDataEncoder().encode(
      args as MintHeroInstructionDataArgs,
    ),
    programAddress,
  } as MintHeroInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountPlayer,
    TAccountHero,
    TAccountSystemProgram
  >);
}

export type MintHeroInput<
  TAccountUser extends string = string,
  TAccountPlayer extends string = string,
  TAccountHero extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  player: Address<TAccountPlayer>;
  hero: Address<TAccountHero>;
  systemProgram?: Address<TAccountSystemProgram>;
  clientSeed: MintHeroInstructionDataArgs["clientSeed"];
};

export function getMintHeroInstruction<
  TAccountUser extends string,
  TAccountPlayer extends string,
  TAccountHero extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HERO_GAME_PROGRAM_ADDRESS,
>(
  input: MintHeroInput<
    TAccountUser,
    TAccountPlayer,
    TAccountHero,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): MintHeroInstruction<
  TProgramAddress,
  TAccountUser,
  TAccountPlayer,
  TAccountHero,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? HERO_GAME_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: true },
    hero: { value: input.hero ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.hero),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getMintHeroInstructionDataEncoder().encode(
      args as MintHeroInstructionDataArgs,
    ),
    programAddress,
  } as MintHeroInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountPlayer,
    TAccountHero,
    TAccountSystemProgram
  >);
}

export type ParsedMintHeroInstruction<
  TProgram extends string = typeof HERO_GAME_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    player: TAccountMetas[1];
    hero: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: MintHeroInstructionData;
};

export function parseMintHeroInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedMintHeroInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      player: getNextAccount(),
      hero: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getMintHeroInstructionDataDecoder().decode(instruction.data),
  };
}
